#requires libffi
#requires pcre
#requires libtool
#requires gettext
#requires pkgconfig

        mcd $BUILDDIR/glib2
        # glib2 expects --enable-threads=yes to enable threads,
        #  anything else disables threads

        $SRC/glib-*/configure `echo $TCONFIGARGS | sed 's/posix/yes/'` --enable-static --disable-modular-tests
        make $J
        make $J install codegen_PYTHON=
