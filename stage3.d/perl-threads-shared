
#requires perl-Carp
#requires perl-threads
#requires perl-ExtUtils-MakeMaker

export RPMBUILD_EXTRA_ARGS="--with bootstrap"

rpmi perl-threads-shared
sed -i -e "s|%{_mandir}/man3|#&|" $HOME/rpmbuild/SPECS/perl-threads-shared.spec
rpmb -bb perl-threads-shared
rpminst perl-threads-shared