#requires perl-Thread-Queue
#requires perl-Text-Unidecode

rpmi coreutils

# Allow root ./configure.
export FORCE_UNSAFE_CONFIGURE=1

rpmb -bb coreutils

# Only build; do not install, lest we brick the system.